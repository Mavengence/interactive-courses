CODEX COURSE — ACCESSIBILITY CONTRAST FIX PASS
Date: 2026-04-27
Status: WCAG 2.1 AA compliant (body text 4.5:1, large text 3:1)

-------------------------------------------------------------------
WHAT CHANGED
-------------------------------------------------------------------
A new stylesheet `styles/a11y.css` has been added as the final layer
of the CSS cascade, wired into index.html and all 12 lesson files.
It fixes every failure found by the DOM contrast auditor.

Load order (low → high priority):
  core.css → lesson.css/home.css → motion.css → prod.css
  → theme.css → a11y.css   ← new, authoritative

-------------------------------------------------------------------
FAILURES FIXED (91 total, 22 unique selectors, 0 remaining)
-------------------------------------------------------------------
1.  Topbar progress badge "0 / 12 completed"
    — rgb(122,131,148) on #FAF9F6   3.63:1  →  #4B5563   6.56:1

2.  Topbar crumb "Track NN · Lesson NN" (all 12 lessons)
    — same fix

3.  Home hero rail eyebrow "codex / course"
    — same fix

4.  Home hero meta strip ("12 lessons  33 drills  ~2h  $0")
    — label spans bumped to #4B5563
    — stat number <b> bumped to #1F2937 (was an almost-invisible #E8ECF2)

5.  Home hero diagram card "the codex loop · every task, every time"
    — muted spans bumped to #4B5563 on the white diagram head
    — dark panel body explicitly set to bg #0B0D10 with text #E8ECF2
      (was rendering black-on-black at 1:1 — every h3, p and code
       inside the diagram is now light-on-dark ≥ 9:1)

6.  Course outline section copy + track status + lesson number +
    lesson sub-copy + lesson meta
    — all muted tints bumped to #4B5563

7.  CTA band "Start at lesson 01…" paragraph
    — bumped to #4B5563

8.  Footer "codex/course · v1.0", "unaffiliated · built as a learning
    artifact", and the "reset progress" link
    — text bumped to #4B5563
    — link turned deep cobalt #0047B3 (7.44:1) with underline

9.  Lesson-chrome "Mark as completed" button (all 12 lessons)
    — bumped to #4B5563 with #374151 hover

10. Lesson 03 bespoke widget: PR branch label "generic codex-refactor"
    — bumped from #6B7280 (4.43:1) to #374151 (9.74:1)

11. Lesson 05 bespoke widget: "sweet spot locked" toast
    — was off-white-on-off-white (1:1); now dark ink pill with
      cream text and subtle shadow (> 14:1)

12. Lesson 07 bespoke widget: syntax-highlighted strings
    — yellow #E6DB74 (1.35:1) → olive-gold #8B6A00 (4.97:1)
    — keywords, comments, numbers, functions all locked to
      AA-compliant tones for defensive future edits

13. Lesson 10 bespoke widget: "Launch" task-card button
    — off-white-on-off-white (1:1); now dark pill with cream text
      and cobalt hover

-------------------------------------------------------------------
VERIFICATION
-------------------------------------------------------------------
Auditor:        run_audit.py
Rule file:      contrast_audit.js
Run:            python3 run_audit.py
Result:         0 failures across 13 pages
Full report:    audit_report.json
Unique fixes:   audit_unique.json (empty after fix)

The fix layer is narrow on purpose — it only targets the specific
selectors that failed and does not touch decorative colors, SVG
icons, or the intentional dark surfaces elsewhere in the course.


===================================================================
PASS 2 — PER-MODULE POLISH PASS
===================================================================
Goal: fix every visible bug on each of the 12 lessons — bullets,
numbering, toggle buttons, card alignment, quiz widgets, bespoke
interactives.

Approach
- One dedicated polish agent per lesson, running in parallel.
- Each agent received: the lesson HTML, the bespoke widget JS
  (js/lessons/LNN.js), the shared styles (lessons.css, widgets.css,
  typography.css, motion.css), and a11y.css.
- Each agent returned a body.lesson-NN scoped CSS patch so rules
  never bleed across lessons.

Integration
- Merged patches into styles/polish.css (≈ 21 KB), loaded AFTER
  a11y.css so it wins the cascade.
- Added class="lesson-NN" to each lesson's <body> so the scoped
  rules actually match.

Bugs fixed per lesson
- L01 (5): sidebar TOC column alignment; equal-height cards; pull-
  quote / callout vertical centering; bespoke buttons; meter label
- L02 (5): quiz option alignment; card heights in "what's in the box";
  sidebar numbering; pull-quote styling; widget layout
- L03 (6): pull-quote / callout vertical alignment; quiz letter offset;
  ordered-list padding; diff-line baseline; mono on diff content; card
  padding
- L04 (4): sidebar label nudge; .tiny font-size in 4-part grid; spec
  toggle; score-bar text
- L05 (5): quiz letter centering; equal-height grid-3 cards; slider
  handle affordance; success readout centering + ok-green; terminal
  error color
- L06 (4): quiz radio alignment; callout icon alignment; checklist row
  centering; PR card stretch fix
- L07 (4): 2-column checklist grid; PR X-Ray wrapper flex-row; pull-
  quote; quiz option wrapping
- L08 (5): 2x2 grid columns; pull-quote alignment; callout spacing;
  quiz baseline; decision-tree font + mono in compare widget
- L09 (5): title line-height; tool-card grid; belt + rack alignment;
  terminal badges; slot spacing
- L10 (5): three-column card sizing; diff compare; team-flow bullets;
  launch button contrast; task-card alignment
- L11 (5): hero meta icon alignment; card-back / drop-zone layout;
  sidebar numbering; pull-quote icon; flashcard flip sizing
- L12 (4): quiz option alignment; clock SVG centering; block column
  gap; left-column sizing

Root-cause fix (from this pass)
- motion.css had `.lesson-content > section { opacity:0 }` gated on
  IntersectionObserver. In some render paths, sections below the fold
  never un-hid. a11y.css now force-reveals them; the entrance animation
  still plays when observed.

Verification
- Re-ran the contrast auditor: 0 failures.
- Full-page 1280 px screenshots of all 12 lessons + homepage reviewed:
  no blank sections, no overlapping widgets, all quiz cards populated,
  all bespoke widgets mount cleanly, navigation and footer consistent.

Files touched
- styles/a11y.css       (pass 1)
- styles/polish.css     (pass 2, NEW)
- index.html            (stylesheet include)
- lessons/*.html (x12)  (stylesheet include + body class="lesson-NN")
